# SliTaz package receipt.

PACKAGE="gecb"
VERSION="0.8.4"
CATEGORY="development"
SHORT_DESC="Graphical Class Browser for GNU/EDMA."
MAINTAINER="hackdorte@sapo.pt"
LICENSE="LGPL"
SOFTWARE="edma"
GNU_FTP="http://ftp.gnu.org/gnu"
TARBALL="$PACKAGE-$VERSION.tar.gz"
WEB_SITE="http://www.gnu.org/software/edma/"
WGET_URL="$GNU_FTP/$SOFTWARE/$TARBALL"
TAGS="browser class edma"

DEPENDS="edma gidfwizard gtk+ libtool"
BUILD_DEPENDS="gtk+-dev gcc autoconf glibc-dev pkg-config automake"

# Rules to configure and make the package.
compile_rules()
{
    ./configure \
        --prefix=/usr \
        $CONFIGURE_ARGS &&
    make $MAKEFLAGS &&
    make DESTDIR=$DESTDIR install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr
	cp -a $install/* $fs/
}
