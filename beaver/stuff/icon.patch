--- a/src/interface.c
+++ b/src/interface.c
@@ -1922,7 +1922,7 @@
 
 	/* set the beaver logo as the default logo for
 	 * all windows to be opened */
-	gtk_window_set_default_icon_from_file (ICON_DIR "/beaver.png", NULL);
+	gtk_window_set_default_icon_name ("beaver");
 
 	/* initialize settings */
 	Settings = init_settings ();
