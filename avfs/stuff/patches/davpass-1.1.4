--- usr/bin/davpass.original
+++ usr/bin/davpass
@@ -72,6 +72,6 @@
 
 if [ "$acct" = @ ] ; then usage; fi
 
-echo $username | cp /dev/stdin $basedir/#dav_ctl:$acct/username
-echo $passwd | cp /dev/stdin $basedir/#dav_ctl:$acct/password
+echo $username | install -D /dev/stdin $basedir/#dav_ctl:$acct/username
+echo $passwd | install -D /dev/stdin $basedir/#dav_ctl:$acct/password
 
