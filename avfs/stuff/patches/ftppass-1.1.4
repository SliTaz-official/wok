--- usr/bin/ftppass.original
+++ usr/bin/ftppass
@@ -35,8 +35,8 @@
 stty $oldsetting
 echo
 
-echo $passwd | cp /dev/stdin $basedir/#ftp_ctl:$userhost/password
+echo $passwd | install -D /dev/stdin $basedir/#ftp_ctl:$userhost/password
 
 if [ -e "$basedir/#ucftp_ctl" ]; then
-    echo $passwd | cp /dev/stdin $basedir/#ucftp_ctl:$userhost/password
+    echo $passwd | install -D /dev/stdin $basedir/#ucftp_ctl:$userhost/password
 fi
