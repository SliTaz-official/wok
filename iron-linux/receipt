# SliTaz package receipt.

PACKAGE="iron-linux"
VERSION="7.0.520.0"
CATEGORY="network"
SHORT_DESC="Secure web browser (Chromium source)."
MAINTAINER="paul@slitaz.org"
DEPENDS="nss-dev libfirefox GConf cups libjpeg62" # may need updating?
TARBALL="$PACKAGE.tar.gz"
WEB_SITE="http://www.srware.net/en/software_srware_iron.php"
WGET_URL="http://www.srware.net/downloads/$TARBALL"

# Rules to configure and make the package.
compile_rules()
{
	cp -a $src $DESTDIR/$PACKAGE
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/lib
	cp -a $_pkg/$PACKAGE $fs/usr/lib
	# Desktop file
	cp -a stuff/* $fs
}

# Create a link to /usr/bin/iron and copy png.
post_install()
{
	cp -a $1/usr/lib/iron-linux/product_logo_48.png $1/usr/share/pixmaps/iron.png
	ln -s $1/usr/lib/iron-linux/iron $1/usr/bin/iron
}

# Remove link and png.
post_remove()
{
	rm -f $1/usr/bin/iron
	rm -f $1/usr/share/pixmaps/iron.png
}
	
