# SliTaz package receipt.

PACKAGE="ethumb"
VERSION="1.7.6"
CATEGORY="graphics"
SHORT_DESC="E17 Thumbnail generation library."
MAINTAINER="pankso@slitaz.org"
TARBALL="$PACKAGE-$VERSION.tar.bz2"
WEB_SITE="http://www.enlightenment.org/"
WGET_URL="http://download.enlightenment.org/releases/$TARBALL"
TAGS="e17"

DEPENDS="eina eet evas ecore edje emotion edbus eio libexif"
BUILD_DEPENDS="eina-dev eet-dev evas-dev ecore-dev edje-dev emotion-dev \
embryo-dev edbus-dev jpeg-dev expat-dev libexif-dev lua5.1-dev eio-dev"

# Rules to configure and make the package.
compile_rules()
{
	./configure \
		--libexecdir=/usr/lib/$PACKAGE \
		--disable-doc \
		--with-internal-maximum-log-level=2 \
		$CONFIGURE_ARGS &&
	make && make install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/lib
	cp -a $install/usr/bin $fs/usr
	cp -a $install/usr/lib/*.so* $fs/usr/lib
	cp -a $install/usr/lib/ethumb $fs/usr/lib
	cp -a $install/usr/share $fs/usr
}
