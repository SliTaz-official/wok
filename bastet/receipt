# SliTaz package receipt.

PACKAGE="bastet"
VERSION="0.41"
CATEGORY="base-apps"
SHORT_DESC="Bastard tetris clone."
MAINTAINER="pankso@slitaz.org"
DEPENDS="ncurses"
TARBALL="$PACKAGE-$VERSION.tgz"
WEB_SITE="http://fph.altervista.org/prog/bastet.shtml"
WGET_URL="http://fph.altervista.org/prog/bastet-0.41.tgz"

# Rules to configure and make the package.
compile_rules()
{
	cd $src
	make
	strip bastet
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/games $fs/var/games
	cp -a $src/bastet $fs/usr/games
	touch $fs/var/games/bastet.scores
	chmod 666 $fs/var/games/bastet.scores
}
