#!/usr/bin/perl

use strict;
use warnings;

use RRD::Simple();

if (@ARGV != 3) {
   print "Usage: $0 rrdfile DSname\n";
}

my $file = $ARGV[0];
my $dsname = $ARGV[1];

my $rrd = RRD::Simple->new();

print "Add $dsname into $file...";
$rrd->add_source($file, 'DS_name' => $dsname);
print " ok.\n";
