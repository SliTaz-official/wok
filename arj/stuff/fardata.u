--- fardata.c
+++ fardata.c
@@ -190,6 +190,7 @@
 
 /* Length-limited strlen() */
 
+#if 0
 static int strnlen(const char FAR *s, int count)
 {
  const char FAR *sc;
@@ -198,6 +199,7 @@
   ;
  return(sc-s);
 }
+#endif
 
 /* Hex representation of digits */
 
