# SliTaz package receipt.

PACKAGE="npth"
VERSION="1.5"
CATEGORY="security"
SHORT_DESC="The new GNU portable threads library"
MAINTAINER="al.bobylev@gmail.com"
LICENSE="GPL2 LGPL3"
WEB_SITE="https://www.gnupg.org/related_software/npth/"
TARBALL="$PACKAGE-$VERSION.tar.bz2"
WGET_URL="https://www.gnupg.org/ftp/gcrypt/npth/$TARBALL"

DEPENDS="glibc-base"

# Rules to configure and make the package.
compile_rules()
{
	# Integrity check: https://www.gnupg.org/download/integrity_check.html
	echo "93ddf1a3bdbca00fb4cf811498094ca61bbb8ee1  $SRC/$TARBALL" | sha1sum -c || exit 1

	./configure $CONFIGURE_ARGS && make && make install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/lib
	cp -a $install/usr/lib/*.so* $fs/usr/lib
}
