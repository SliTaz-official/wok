Swap partitions may have neither label nor uuid
--- busybox-1.20.0/util-linux/volume_id/get_devname.c
+++ busybox-1.20.0/util-linux/volume_id/get_devname.c
@@ -50,5 +50,5 @@
 		goto ret;
 
-	if (vid->label[0] != '\0' || vid->uuid[0] != '\0') {
+	if (vid->type) {
 		*label = xstrndup(vid->label, sizeof(vid->label));
 		*uuid  = xstrndup(vid->uuid, sizeof(vid->uuid));
