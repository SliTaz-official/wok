keep previous default behaviour
--- busybox-1.22.0/util-linux/losetup.c
+++ busybox-1.22.0/util-linux/losetup.c
@@ -70,7 +70,7 @@
 	}
 
 	/* -a */
-	if (opt == OPT_a) {
+	if (opt == OPT_a || (!opt && !argv[0])) {
 		int n;
 		for (n = 0; n < MAX_LOOP_NUM; n++) {
 			char *s;
