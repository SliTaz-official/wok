Basename may spuriously break build using 'set -e'
--- busybox-1.12.0/coreutils/basename.c
+++ busybox-1.12.0/coreutils/basename.c
@@ -48,5 +48,6 @@
 
 	/* puts(s) will do, but we can do without stdio this way: */
 	s[m++] = '\n';
-	return full_write(STDOUT_FILENO, s, m) == (ssize_t)m;
+	full_write(STDOUT_FILENO, s, m);
+	return 0;
 }
