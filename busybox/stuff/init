#!/bin/sh

echo "switching / to tmpfs..."
/bin/mount -t tmpfs tmpfs /mnt
/bin/mkdir /mnt/mnt
/bin/mv /dev /usr /var /root /etc /sys /tmp /media /proc /home /mnt
/bin/ln -s bin/busybox mnt/init
/bin/cp -a /sbin /bin /lib /mnt
exec /sbin/switch_root mnt /init
