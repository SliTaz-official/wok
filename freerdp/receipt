# SliTaz package receipt.

PACKAGE="freerdp"
VERSION="1.0.1"
CATEGORY="network"
SHORT_DESC="Remote Desktop Protocol (RDP) client for Windows terminal server"
MAINTAINER="pascal.bellard@slitaz.org"
TARBALL="$PACKAGE-$VERSION.tar.gz"
WEB_SITE="http://www.freerdp.com/"
WGET_URL="https://github.com/downloads/FreeRDP/FreeRDP/$TARBALL"
DEPENDS="zlib libkrb5 libssl libcomerr cups alsa-lib libxcb xorg-libXcursor \
ffmpeg xorg-libXv"
BUILD_DEPENDS="zlib-dev openssl-dev cups-dev alsa-lib-dev libxcb-dev xorg-dev \
xorg-dev-proto wget cmake ffmpeg-dev xorg-libXv-dev"
TAGS="rdp remote-desktop"

# Rules to configure and make the package.
compile_rules()
{
	cd $src
	cmake -DWITH_PCSC=OFF -DCMAKE_INSTALL_PREFIX=/usr . &&
	make &&
	make DESTDIR=$DESTDIR install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/lib/freerdp $fs/usr/share
	cp -a $install/usr/bin $fs/usr
	cp -a $install/usr/share/freerdp $fs/usr/share
	cp -a $install/usr/lib/*.so* $fs/usr/lib
	cp -a $install/usr/lib/freerdp/*.so* $fs/usr/lib/freerdp
}
