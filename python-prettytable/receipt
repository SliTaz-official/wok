# SliTaz package receipt.

PACKAGE="python-prettytable"
VERSION="0.6.1"
SOURCE="prettytable"
CATEGORY="system-tools"
SHORT_DESC="Python library for easily displaying tabular data in ASCII."
MAINTAINER="pankso@slitaz.org"
LICENSE="BSD"
TARBALL="${SOURCE}-${VERSION}.tar.bz2"
WEB_SITE="http://pypi.python.org/pypi/PrettyTable"
WGET_URL="http://pypi.python.org/packages/source/P/PrettyTable/$TARBALL"

DEPENDS="python"
BUILD_DEPENDS="python-dev python-distribute python-setuptools"

# Rules to configure and make the package.
compile_rules()
{
	python setup.py install --root="$DESTDIR"
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr
	cp -a $install/usr/lib $fs/usr
}

