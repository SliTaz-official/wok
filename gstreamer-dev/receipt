# SliTaz package receipt.

PACKAGE="gstreamer-dev"
VERSION="0.10.36"
SERIES="0.10"
CATEGORY="development"
SHORT_DESC="gstreamer devel files"
MAINTAINER="jozee@slitaz.org"
DEPENDS="gstreamer libxml2-dev"
WANTED="gstreamer"
WEB_SITE="http://gstreamer.freedesktop.org/"

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/lib/gstreamer-$SERIES $fs/usr/share
	mv $install/usr/include $fs/usr
	mv $install/usr/lib/pkgconfig $fs/usr/lib
	mv $install/usr/lib/*.*a $fs/usr/lib/
	mv $install/usr/lib/gstreamer-$SERIES/*.*a $fs/usr/lib/gstreamer-$SERIES
	mv $install/usr/share/aclocal $fs/usr/share
	mv $install/usr/share/gir-1.0 $fs/usr/share
	cd $fs/usr/include
	ln -s gstreamer-$SERIES/gst gst
}
