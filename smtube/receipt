# SliTaz package receipt.

PACKAGE="smtube"
VERSION="15.5.17"
CATEGORY="multimedia"
SHORT_DESC="Qt Youtube browser using *mplayer or vlc."
MAINTAINER="psychomaniak@xakep.ru"
LICENSE="GPL2"
TARBALL="$PACKAGE-$VERSION.tar.bz2"
WEB_SITE="http://smtube.sourceforge.net/"
SF_MIRROR="http://downloads.sourceforge.net"
WGET_URL="$SF_MIRROR/$PACKAGE/$TARBALL"

DEPENDS="mplayer-opt libQtGui libQtXml libQtScript libQtNetwork libQtWebkit"
BUILD_DEPENDS="wget qmake Qt4-dev"

# Rules to configure and make the package.
compile_rules()
{
	cd $src	
	make PREFIX=/usr &&
	make PREFIX=/usr DESTDIR=$DESTDIR install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	cp -a $install/* $fs

	for size in 16 48 64 128 256; do
		rm -rf $fs/usr/share/icons/hicolor/${size}x$size
	done
}
