--- linux-2.6.22.9/lib/decompress_unlzma.c
+++ linux-2.6.22.9/lib/decompress_unlzma.c
@@ -73,6 +73,7 @@
 
 #endif
 
+#define CONFIG_FEATURE_LZMA_FAST
 #include <linux/decompress_unlzma.h>
 
 #define	MIN(a,b) (((a)<(b))?(a):(b))
