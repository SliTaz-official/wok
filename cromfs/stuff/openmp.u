--- cromfs-1.5.4/configure
+++ cromfs-1.5.4/configure
@@ -79,4 +79,3 @@
 
-do_echo -n "Checking if your compiler supports -fopenmp... "
-if cc_check '<stdio.h>' '-fopenmp' 'int x,y[100];for(x=0; x<100;++x)y[x]=5;'; then
+if false; then
   do_echo Yes
