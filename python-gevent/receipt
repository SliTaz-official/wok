# SliTaz package receipt.

PACKAGE="python-gevent"
SOURCE="gevent"
VERSION="0.13.8"
CATEGORY="development"
SHORT_DESC="A coroutine-based Python networking library."
MAINTAINER="pascal.bellard@slitaz.org"
LICENSE="MIT"
TARBALL="$SOURCE-$VERSION.tar.gz"
WEB_SITE="http://www.gevent.org/"
WGET_URL="https://pypi.python.org/packages/source/${SOURCE:0:1}/$SOURCE/$TARBALL"

DEPENDS="python"
BUILD_DEPENDS="wget python-dev python libevent-dev"

# Rules to configure and make the package.
compile_rules()
{
	python setup.py build &&
	python setup.py install --root=$DESTDIR
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs
	cp -a $install/usr $fs
}
