# SliTaz package receipt.

PACKAGE="imagemagick"
VERSION="6.5.5-6"
SHORT_VERSION="6.5.5"
CATEGORY="graphics"
SHORT_DESC="Images manipulation programs."
MAINTAINER="pankso@slitaz.org"
DEPENDS="xorg jpeg libpng tiff libxml2 freetype libgomp bzlib expat \
fontconfig lcms jasper graphviz librsvg libgsf libgio cairo-xcb gcc-lib-base"
BUILD_DEPENDS="jpeg-dev libpng-dev tiff-dev libxml2-dev lcms graphviz-dev"
SOURCE="ImageMagick"
TARBALL="$SOURCE-$VERSION.tar.bz2"
WEB_SITE="http://www.imagemagick.org/"
WGET_URL="ftp://ftp.imagemagick.org/pub/ImageMagick/$TARBALL"
TAGS="image photo toolkit"

# Rules to configure and make the package.
compile_rules()
{
	cd $src
	./configure \
		--prefix=/usr \
		--infodir=/usr/share/info \
		--mandir=/usr/share/man \
		--without-perl \
		$CONFIGURE_ARGS &&
	make &&
	make DESTDIR=$PWD/_pkg install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/lib $fs/usr/share
	cp -a $_pkg/usr/bin $fs/usr
	cp -a $_pkg/usr/lib/*.so* $fs/usr/lib
	cp -a $_pkg/usr/lib/$SOURCE-$SHORT_VERSION $fs/usr/lib
	cp -a $_pkg/usr/share/$SOURCE-$SHORT_VERSION $fs/usr/share
}

