# SliTaz package receipt.

PACKAGE="imagemagick"
VERSION="6.7.6-10"
SHORT_VERSION="${VERSION%-*}"
CATEGORY="graphics"
SHORT_DESC="Images manipulation programs."
MAINTAINER="pankso@slitaz.org"
DEPENDS="xorg-libXext jpeg libpng tiff libxml2 freetype libgomp bzlib expat \
fontconfig lcms jasper ilmbase openexr graphviz librsvg libgsf libgio cairo \
pango gcc-lib-base jbigkit libcroco fftw"
BUILD_DEPENDS="xorg-libXext-dev jpeg-dev libpng-dev tiff-dev libxml2-dev \
freetype-dev bzip2-dev expat-dev fontconfig-dev lcms-dev jasper-dev \
ilmbase-dev openexr-dev graphviz-dev librsvg-dev libgsf-dev libgio-dev \
cairo-dev pango-dev libcroco-dev fftw-dev util-linux-uuid-dev libtool"
SOURCE="ImageMagick"
TARBALL="$SOURCE-$VERSION.tar.xz"
WEB_SITE="http://www.imagemagick.org/"
WGET_URL="ftp://ftp.imagemagick.org/pub/ImageMagick/legacy/$TARBALL"
TAGS="image photo toolkit"

# Rules to configure and make the package.
compile_rules()
{
	cd $src
	./configure \
		--prefix=/usr \
		--infodir=/usr/share/info \
		--mandir=/usr/share/man \
		--sysconfdir=/etc \
		--without-perl \
		--with-magick-plus-plus \
		--with-modules \
		--enable-hdri \
		$CONFIGURE_ARGS &&
	make $MAKEFLAGS &&
	make DESTDIR=$DESTDIR install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/lib $fs/usr/share
	cp -a $_pkg/etc $fs
	cp -a $_pkg/usr/bin $fs/usr
	cp -a $_pkg/usr/lib/*.so* $fs/usr/lib
	cp -a $_pkg/usr/lib/$SOURCE-$SHORT_VERSION $fs/usr/lib
	cp -a $_pkg/usr/share/$SOURCE-$SHORT_VERSION $fs/usr/share
}

