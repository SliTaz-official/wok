This package is a part of GNU gettext, it contains:

  * `xgettext`: to collect translations from sources to new POT file
  * `msgmerge`: to update existing PO files with the new POT file
  * `msgfmt`:   to compile MO files from PO files
