# SliTaz package receipt.

PACKAGE="pangomm"
VERSION="2.24.0"
CATEGORY="x-window"
SHORT_DESC="Pango binding for GTKmm."
MAINTAINER="pankso@slitaz.org"
DEPENDS="pixman cairomm glibmm libgiomm pango gcc-lib-base"
BUILD_DEPENDS="pixman-dev cairomm-dev glibmm-dev libgiomm-dev"
TARBALL="$PACKAGE-$VERSION.tar.gz"
WEB_SITE="http://ftp.gnome.org/pub/gnome/sources/pangomm/"
WGET_URL="http://ftp.gnome.org/pub/gnome/sources/pangomm/2.24/$TARBALL"

# Rules to configure and make the package.
compile_rules()
{
    cd $src
    ./configure \
    	--prefix=/usr \
    	--mandir=/usr/share/man \
    	$CONFIGURE_ARGS &&
    make &&
    make DESTDIR=$PWD/_pkg install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/lib
	cp -a $_pkg/usr/lib/*.so* $fs/usr/lib
}
