# SliTaz package receipt.

PACKAGE="python-http-parser"
SOURCE="http-parser"
VERSION="0.8.1"
CATEGORY="development"
SHORT_DESC="HTTP request/response parser for python in C"
MAINTAINER="claudinei@slitaz.org"
TARBALL="$VERSION.zip"
WEB_SITE="https://github.com/benoitc/http-parser/"
WGET_URL="https://github.com/benoitc/$SOURCE/archive/$TARBALL"

DEPENDS="python"
BUILD_DEPENDS="python-dev setuptools wget"

# Rules to configure and make the package.
compile_rules()
{
	cd $src
	python setup.py install --root=$DESTDIR
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr
	cp -a $install/usr $fs
}

