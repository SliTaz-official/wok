# SliTaz package receipt.

PACKAGE="tuxpaint"
VERSION="0.9.21"
CATEGORY="graphics"
SHORT_DESC="Kid painting programm."
MAINTAINER="pankso@slitaz.org"
TARBALL="$PACKAGE-$VERSION.tar.gz"
WEB_SITE="http://tuxpaint.org/"
WGET_URL="http://downloads.sourceforge.net/sourceforge/$PACKAGE/$TARBALL"

DEPENDS="libpng fribidi librsvg cairo libsdl-mixer libsdl-pango libsdl-image \
libsdl-ttf libpaper python"
BUILD_DEPENDS="libpng-dev fribidi-dev librsvg-dev cairo-dev libsdl-dev \
libsdl-mixer-dev libsdl-pango-dev libsdl-image-dev libsdl-ttf-dev \
libpaper-dev python-dev"

# Rules to configure and make the package.
compile_rules()
{
	cd $src
	sed -i 's/$(ARCH_LINKS)/$(ARCH_LINKS) -lpng/' Makefile
	make PREFIX=/usr
	make PREFIX=/usr DESTDIR=$DESTDIR install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/share
	cp -a $install/etc $fs
	cp -a $install/usr/bin $fs/usr
	cp -a $install/usr/lib $fs/usr
	cp -a $install/usr/share/tuxpaint $fs/usr/share
}
