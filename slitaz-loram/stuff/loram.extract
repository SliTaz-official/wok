cromfs=".usr.cromfs"
rmdir usr
bin/unmkcromfs $cromfs usr
rm -f $cromfs
( cd usr/.moved ; find * -print ) | \
while read file; do
	[ -L "$file" ] || continue
	rm -f "$file"
	mv "usr/.moved/$file" "$file"
done
rm -rf usr/.moved
