# SliTaz package receipt.

PACKAGE="njconnect"
VERSION="1.2"
CATEGORY="multimedia"
SHORT_DESC="njconnect - Jack connection manager (ncurses)"
MAINTAINER="xj@wp.pl"
WEB_SITE="http://njconnect.sourceforge.net"
TARBALL="$PACKAGE-$VERSION.tar.xz"
WGET_URL="http://downloads.sourceforge.net/project/$PACKAGE/$PACKAGE-$VERSION.tar.xz"

DEPENDS="jack-audio-connection-kit ncurses"
BUILD_DEPENDS="jack-audio-connection-kit-dev ncurses-dev"

# Rules to configure and make the package.
compile_rules()
{
	tar xf $SOURCES_REPOSITORY/$TARBALL -C $WOK/$PACKAGE/source
	cd $src
	make DESTDIR=$install install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	cp -a $install/* $fs
}
