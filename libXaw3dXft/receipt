# SliTaz package receipt.

PACKAGE="libXaw3dXft"
VERSION="1.6.2"
CATEGORY="x-window"
SHORT_DESC="Xpaint module"
MAINTAINER="pascal.bellard@slitaz.org"
WEB_SITE="http://sourceforge.net/projects/sf-xpaint/"
TARBALL="$PACKAGE-$VERSION.tar.bz2"
WGET_URL="$SF_MIRROR/sf-xpaint/$TARBALL"

DEPENDS="xorg-libXmu fontconfig xorg-libXrender xorg-libXft lzlib"
BUILD_DEPENDS="xorg-libXmu-dev util-linux-uuid-dev expat-dev"

# Rules to configure and make the package.
compile_rules()
{
	./configure $CONFIGURE_ARGS && make && make install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
        mkdir -p $fs/usr/lib
        cp -a $install/usr/lib/*.so* $fs/usr/lib
}
