# SliTaz package receipt.

PACKAGE="sylpheed"
VERSION="3.0.2"
CATEGORY="network"
SHORT_DESC="Simple, lightweight but featureful, and easy-to-use e-mail client"
MAINTAINER="fraazz@altern.org"
DEPENDS="gtk+ libssl gpgme gnupg xorg-libXdamage"
BUILD_DEPENDS="openssl-dev glib glib-dev"
TARBALL="$PACKAGE-$VERSION.tar.bz2"
WEB_SITE="http://sylpheed.sraoss.jp/en/"
WGET_URL="http://sylpheed.sraoss.jp/sylpheed/v3.0/$TARBALL"
TAGS="mail internet"

# Rules to configure and make the package.
compile_rules()
{
	cd $src
	./configure \
		--prefix=/usr \
		--infodir=/usr/share/info \
		--mandir=/usr/share/man \
		--disable-gtkspell \
		$CONFIGURE_ARGS &&
	make &&
	make DESTDIR=$PWD/_pkg install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/lib
	cp -a $_pkg/usr/bin $fs/usr
	cp -a $_pkg/usr/lib/*.so* $fs/usr/lib
}
