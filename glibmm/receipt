# SliTaz package receipt.

PACKAGE="glibmm"
VERSION="2.24.2"
CATEGORY="x-window"
SHORT_DESC="C++ interface for the popular GUI library GTK+."
MAINTAINER="pankso@slitaz.org"
DEPENDS="glib libgio libgiomm libsigc++ gcc-lib-base"
BUILD_DEPENDS="glib-dev libgio-dev libgiomm-dev libsigc++-dev"
TARBALL="$PACKAGE-$VERSION.tar.gz"
WEB_SITE="http://www.gtkmm.org/"
WGET_URL="http://ftp.gnome.org/pub/GNOME/sources/glibmm/2.24/$TARBALL"

# Rules to configure and make the package.
compile_rules()
{
	cd $src
	./configure \
		--prefix=/usr \
		--infodir=/usr/share/info \
		--mandir=/usr/share/man \
		$CONFIGURE_ARGS &&
	make &&
	make DESTDIR=$PWD/_pkg install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/lib
	cp -a $_pkg/usr/lib/*.so* $fs/usr/lib
	# Remove libgiomm
	rm -rf $fs/usr/lib/libgiomm*
	# Cook all packages based on glibmm
	for i in $(cd $WOK; grep -l '^WANTED="glibmm"$' */receipt)
	do
		echo tazwok cook ${i%/receipt}
	done
}
