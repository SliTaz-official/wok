# SliTaz package receipt.

PACKAGE="openvpn"
VERSION="2.3.2"
CATEGORY="network"
SHORT_DESC="Full-featured open source SSL VPN solution."
MAINTAINER="pascal.bellard@slitaz.org"
LICENSE="GPL2"
TARBALL="$PACKAGE-$VERSION.tar.gz"
WEB_SITE="http://openvpn.net/"
WGET_URL="http://swupdate.openvpn.net/community/releases/$TARBALL"
TAGS="vpn tunnel network"
HOST_ARCH="i486 arm"

DEPENDS="lzo libcrypto openssl"
BUILD_DEPENDS="lzo-dev libcrypto-dev openssl-dev pam-dev"

# Rules to configure and make the package.
compile_rules()
{
	cd $src
	./configure \
		--prefix=/usr \
		--enable-password-save \
		$CONFIGURE_ARGS &&
	make &&
	( cd plugin/auth-pam/ ; make ) &&
	( cd plugin/down-root/ ; make ) &&
	make DESTDIR=$DESTDIR install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/sbin
	cp -a $install/usr/sbin/openvpn $fs/usr/sbin
}

# Post install/remove commands for Tazpkg.
post_install()
{
	mkdir -p ${root}/etc/openvpn ${root}/var/run/openvpn
}

post_remove()
{
	# remove config and pids
	rm -rf ${root}/etc/openvpn ${root}/var/run/openvpn
}

