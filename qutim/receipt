# SliTaz package receipt.

PACKAGE="qutim"
VERSION="0.3.1"
CATEGORY="network"
SHORT_DESC="Multiprotocol IM, written in qt4"
MAINTAINER="devl547@gmail.com"
TARBALL="$PACKAGE-$VERSION.tar.bz2"
WEB_SITE="http://qutim.org"
WGET_URL="$WEB_SITE/dwnl/40/$TARBALL"

DEPENDS="libQtCore libQtGui libQtNetwork"
BUILD_DEPENDS="Qt4-dev qmake cmake dbus-dev"

# Rules to configure and make the package.
compile_rules()
{
	cd $src
	mkdir ../qutim-build && cd ../qutim-build
	cmake ../$PACKAGE-$VERSION -DCMAKE_INSTALL_PREFIX=/usr &&
	make $MAKEOPTS &&
	make DESTDIR=$DESTDIR install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	mkdir -p $fs/usr/
	cp -a $install/usr/bin $fs/usr
	cp -a $install/usr/lib $fs/lib
	cp -a $install/usr/share $fs/usr
}
