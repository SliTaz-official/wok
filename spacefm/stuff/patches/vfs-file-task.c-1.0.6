--- vfs-file-task.c.orig
+++ vfs-file-task.c
@@ -1799,6 +1799,12 @@
             argv[a++] = g_strdup( "SpaceFM Command" );
             single_arg = TRUE;
         }
+        else if ( !strcmp( use_su, "/usr/bin/tazbox" ) )
+        {
+            // tazbox
+            argv[a++] = g_strdup( "su" );
+            single_arg = TRUE;
+        }
         else if ( strstr( use_su, "kdesu" ) )
         {
             // kdesu kdesudo
